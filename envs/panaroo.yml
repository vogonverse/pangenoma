name: panaroo
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - panaroo>=1.3
  - mafft>=7.490
  - cd-hit
  - python>=3.9
  - numpy<1.24  # Fix for panaroo incompatibility with numpy.tostring() removal